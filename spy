-- [[ KRALLDEN SPY v10.7 - ULTIMATE PARSER & EXECUTE FIXED ]] --

local player = game:GetService("Players").LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

if playerGui:FindFirstChild("KralldenSpyUI") then playerGui.KralldenSpyUI:Destroy() end

local ScreenGui = Instance.new("ScreenGui", playerGui)
ScreenGui.Name = "KralldenSpyUI"; ScreenGui.ResetOnSpawn = false; ScreenGui.DisplayOrder = 2147483647

local Main = Instance.new("Frame", ScreenGui)
Main.BackgroundColor3 = Color3.fromRGB(15, 15, 20); Main.Size = UDim2.new(0, 820, 0, 440)
Main.Position = UDim2.new(0.5, -410, 0.5, -220); Main.Active = true; Main.Draggable = true; Main.BorderSizePixel = 0

local MainMemory, PathFilter, ManualBannedPaths = {}, {}, {}
local AntiSpamCooldowns, AntiSpamCounts = {}, {}
local selfMode, controlMode, antiSpam = true, true, false
local spyFS, spyFC, spyIS = true, false, false
local currentSelectionGUID, lastCount = nil, 0

-- [[ УЛУЧШЕННЫЙ ПАРСЕР ИЗ ПРОШЛЫХ ВЕРСИЙ ]] --
local function parseTable(val, depth)
    depth = depth or 0
    local t = typeof(val)
    if t == "string" then return '"' .. val .. '"'
    elseif t == "number" or t == "boolean" then return tostring(val)
    elseif t == "Vector3" then return "Vector3.new(" .. tostring(val) .. ")"
    elseif t == "Vector2" then return "Vector2.new(" .. tostring(val) .. ")"
    elseif t == "CFrame" then return "CFrame.new(" .. tostring(val) .. ")"
    elseif t == "Color3" then return "Color3.new(" .. tostring(val) .. ")"
    elseif t == "Instance" then 
        local p = ""; pcall(function() 
            local o = val; while o and o ~= game do 
                local n = o.Name; p = (n:match("[%s%W]") and '["'..n..'"]' or n)..(p ~= "" and "."..p or ""); o = o.Parent 
            end 
        end)
        return p ~= "" and "game." .. p:gsub("%.%[", "[") or "nil"
    elseif t == "table" then
        if depth > 4 then return "{...}" end
        local res, count = "{", 0
        for k, v in pairs(val) do
            count = count + 1; if count > 15 then res = res .. "... " break end
            local key = type(k) == "string" and '["'..k..'"]' or "["..tostring(k).."]"
            res = res .. key .. " = " .. parseTable(v, depth + 1) .. ", "
        end
        return count > 0 and res:sub(1, #res-2) .. "}" or "{}"
    end
    return tostring(val)
end

local function generateGUID() return tostring(tick()) .. "-" .. tostring(math.random(1, 100000)) end

-- [[ UI ELEMENTS ]] --
local Header = Instance.new("Frame", Main)
Header.Size = UDim2.new(1, 0, 0, 35); Header.BackgroundColor3 = Color3.fromRGB(25, 25, 30); Header.BorderSizePixel = 0

local Title = Instance.new("TextLabel", Header)
Title.Size = UDim2.new(0, 200, 1, 0); Title.Position = UDim2.new(0, 15, 0, 0); Title.BackgroundTransparency = 1; Title.Text = "KRALLDEN SPY v10.7"; Title.TextColor3 = Color3.new(1, 1, 1); Title.Font = Enum.Font.SourceSansBold; Title.TextSize = 16; Title.TextXAlignment = 0

local function createHeaderBtn(text, offset, color)
    local b = Instance.new("TextButton", Header); b.Size = UDim2.new(0, 95, 0, 24); b.Position = UDim2.new(1, offset, 0.5, -12); b.BackgroundColor3 = color; b.Text = text; b.TextColor3 = Color3.new(1,1,1); b.Font = Enum.Font.SourceSansBold; b.TextSize = 10; b.BorderSizePixel = 0; return b
end

local ControlBtn = createHeaderBtn("CONTROL: ON", -145, Color3.fromRGB(150, 50, 255))
local SelfBtn = createHeaderBtn("SELF: ON", -245, Color3.fromRGB(45, 90, 45))
local AntiSpamBtn = createHeaderBtn("ANTI-SPAM: OFF", -345, Color3.fromRGB(180, 150, 40))
local BlockBtn = createHeaderBtn("BLOCK EVENT", -445, Color3.fromRGB(150, 50, 50))

local ContentFrame = Instance.new("Frame", Main)
ContentFrame.Size = UDim2.new(1, 0, 1, -35); ContentFrame.Position = UDim2.new(0, 0, 0, 35); ContentFrame.BackgroundTransparency = 1

local Scroll = Instance.new("ScrollingFrame", ContentFrame)
Scroll.Position = UDim2.new(0, 8, 0, 8); Scroll.Size = UDim2.new(0, 190, 1, -16); Scroll.BackgroundColor3 = Color3.fromRGB(20, 20, 25); Scroll.BorderSizePixel = 0; Scroll.ScrollBarThickness = 4; Scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
Instance.new("UIListLayout", Scroll).SortOrder = Enum.SortOrder.LayoutOrder

local Details = Instance.new("TextBox", ContentFrame)
Details.Position = UDim2.new(0, 205, 0, 8); Details.Size = UDim2.new(0, 448, 0, 255); Details.BackgroundColor3 = Color3.fromRGB(10, 10, 12); Details.TextColor3 = Color3.new(1, 1, 1); Details.MultiLine = true; Details.TextEditable = false; Details.Font = Enum.Font.Code; Details.TextSize = 11; Details.TextXAlignment = 0; Details.TextYAlignment = 0; Details.ClearTextOnFocus = false; Details.TextWrapped = true

-- [[ LOGIC ]] --
local function addLog(rem, args, isSelf, typeLabel)
    if (typeLabel == "FS" and not spyFS) or (typeLabel == "FC" and not spyFC) or (typeLabel == "IS" and not spyIS) then return end
    local eventPath = parseTable(rem)
    if ManualBannedPaths[eventPath] then return end
    if controlMode and PathFilter[eventPath] then return end 

    local argStrings = {}
    for i, v in pairs(args) do table.insert(argStrings, parseTable(v)) end
    local finalArgs = table.concat(argStrings, ", ")
    
    local logDetails = string.format("Type: %s\nPath: %s\nArgs: %s\n\nScript:\n%s:%s(%s)", 
        typeLabel, eventPath, finalArgs, eventPath, (typeLabel == "IS" and "InvokeServer" or "FireServer"), finalArgs)

    local data = { guid = generateGUID(), name = tostring(rem.Name), type = typeLabel, isSelf = isSelf, fullText = logDetails }
    
    if controlMode then PathFilter[eventPath] = true end
    if isSelf then table.insert(MainMemory, 1, data) else table.insert(MainMemory, data) end
end

-- HOOKS
local mt = getrawmetatable(game); local old = mt.__namecall; setreadonly(mt, false)
mt.__namecall = newcclosure(function(self, ...)
    local m = getnamecallmethod(); local a = {...}; local s = checkcaller()
    if m == "FireServer" then task.spawn(addLog, self, a, s, "FS")
    elseif m == "FireClient" then task.spawn(addLog, self, a, s, "FC")
    elseif m == "InvokeServer" then task.spawn(addLog, self, a, s, "IS") end
    return old(self, ...)
end); setreadonly(mt, true)

-- [[ КНОПКИ ДЕЙСТВИЙ ]] --
local function createBotBtn(text, pos, color)
    local b = Instance.new("TextButton", ContentFrame); b.Size = UDim2.new(0, 220, 0, 58); b.Position = pos; b.BackgroundColor3 = color; b.Text = text; b.TextColor3 = Color3.new(1,1,1); b.Font = Enum.Font.SourceSansBold; b.TextSize = 14; b.BorderSizePixel = 0; return b
end

createBotBtn("COPY ARGS", UDim2.new(0, 205, 0.68, 0), Color3.fromRGB(45, 90, 45)).MouseButton1Click:Connect(function() 
    setclipboard(Details.Text:match("Args: (.-)\n\nScript") or "") 
end)

createBotBtn("COPY SCRIPT", UDim2.new(0, 205, 0.83, 0), Color3.fromRGB(60, 60, 120)).MouseButton1Click:Connect(function() 
    setclipboard(Details.Text:match("Script:\n(.*)") or "") 
end)

createBotBtn("CLEAR LOG", UDim2.new(0, 432, 0.68, 0), Color3.fromRGB(80, 80, 85)).MouseButton1Click:Connect(function() 
    MainMemory = {}; PathFilter = {}; Details.Text = ""; lastCount = -1 
end)

local ExecBtn = createBotBtn("EXECUTE", UDim2.new(0, 432, 0.83, 0), Color3.fromRGB(120, 60, 60))
ExecBtn.MouseButton1Click:Connect(function()
    local code = Details.Text:match("Script:\n(.*)")
    if code and code ~= "" then
        local func, err = loadstring(code)
        if func then func() else warn("Spy Error: " .. tostring(err)) end
    end
end)

-- [[ ЦИКЛ ОБНОВЛЕНИЯ UI ]] --
task.spawn(function()
    while task.wait(0.5) do
        if #MainMemory == lastCount then continue end
        lastCount = #MainMemory; for _, v in pairs(Scroll:GetChildren()) do if v:IsA("TextButton") then v:Destroy() end end
        for _, d in ipairs(MainMemory) do
            local b = Instance.new("TextButton", Scroll); b.Size = UDim2.new(1, -6, 0, 30)
            b.Text = string.format("[%s]%s %s", d.type, (d.isSelf and " [S]" or ""), d.name)
            
            -- Подсветка: Фиолетовый (выбран), Зеленый (твой), Серый (чужой)
            if currentSelectionGUID == d.guid then
                b.BackgroundColor3 = Color3.fromRGB(100, 50, 200)
            elseif d.isSelf then
                b.BackgroundColor3 = Color3.fromRGB(45, 90, 45)
            else
                b.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
            end
            
            b.TextColor3 = Color3.new(1,1,1); b.BorderSizePixel = 0
            b.MouseButton1Click:Connect(function()
                currentSelectionGUID = d.guid; Details.Text = d.fullText; lastCount = -1
            end)
        end
    end
end)
