local playerGui = game:GetService("Players").LocalPlayer:WaitForChild("PlayerGui")
if playerGui:FindFirstChild("QuantumSpyUI") then playerGui.QuantumSpyUI:Destroy() end

local ScreenGui = Instance.new("ScreenGui", playerGui)
ScreenGui.Name = "QuantumSpyUI"
ScreenGui.DisplayOrder = 2147483647

local Main = Instance.new("Frame", ScreenGui)
Main.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Main.Size = UDim2.new(0, 600, 0, 400)
Main.Position = UDim2.new(0.5, -300, 0.5, -200)
Main.Active = true; Main.Draggable = true

-- ЗАГОЛОВОК
local Header = Instance.new("Frame", Main)
Header.Size = UDim2.new(1, 0, 0, 30)
Header.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
local Title = Instance.new("TextLabel", Header)
Title.Size = UDim2.new(1, 0, 1, 0); Title.BackgroundTransparency = 1
Title.Text = "QUANTUM SPY v4.0 (FULL)"; Title.TextColor3 = Color3.new(1, 1, 1)

-- ОСНОВНОЙ СПИСОК
local Scroll = Instance.new("ScrollingFrame", Main)
Scroll.Position = UDim2.new(0, 5, 0, 35); Scroll.Size = UDim2.new(0, 220, 1, -40)
Scroll.CanvasSize = UDim2.new(0, 0, 100, 0)
Instance.new("UIListLayout", Scroll).SortOrder = Enum.SortOrder.LayoutOrder

-- СПАМ СПИСОК (Красная зона)
local SpamFrame = Instance.new("ScrollingFrame", Main)
SpamFrame.Size = UDim2.new(0, 150, 1, -40)
SpamFrame.Position = UDim2.new(1, 5, 0, 35)
SpamFrame.BackgroundColor3 = Color3.fromRGB(40, 20, 20)
SpamFrame.CanvasSize = UDim2.new(0, 0, 10, 0)
Instance.new("UIListLayout", SpamFrame).SortOrder = Enum.SortOrder.LayoutOrder

local Details = Instance.new("TextBox", Main)
Details.Position = UDim2.new(0, 230, 0, 35); Details.Size = UDim2.new(1, -235, 0.5, 0)
Details.BackgroundColor3 = Color3.fromRGB(15, 15, 15); Details.TextColor3 = Color3.new(0.8, 0.8, 0.8)
Details.MultiLine = true; Details.TextXAlignment = 0; Details.TextYAlignment = 0
Details.Text = "Waiting..."; Details.ClearTextOnFocus = false; Details.TextWrapped = true

local CopyArgs = Instance.new("TextButton", Main)
CopyArgs.Position = UDim2.new(0, 230, 0.62, 0); CopyArgs.Size = UDim2.new(1, -235, 0.14, 0)
CopyArgs.BackgroundColor3 = Color3.fromRGB(50, 100, 50); CopyArgs.Text = "COPY ARGS"; CopyArgs.TextColor3 = Color3.new(1, 1, 1)

local CopyScript = Instance.new("TextButton", Main)
CopyScript.Position = UDim2.new(0, 230, 0.80, 0); CopyScript.Size = UDim2.new(1, -235, 0.14, 0)
CopyScript.BackgroundColor3 = Color3.fromRGB(60, 60, 120); CopyScript.Text = "COPY SCRIPT"; CopyScript.TextColor3 = Color3.new(1, 1, 1)

-- ЛОГИКА
local callCounts, lastTime, loggedKeys, blacklisted = {}, {}, {}, {}
local curArgs, curScript = "", ""

local function getSafePath(obj)
    local s, res = pcall(function() return obj:GetFullName() end)
    return s and res or "Access_Denied"
end

local function formatVal(v)
    local t = typeof(v)
    if t == "table" then return "{table}"
    elseif t == "Instance" then return getSafePath(v)
    elseif t == "string" then return '"'..v..'"'
    else return tostring(v) end
end

local function addLog(rem, args, isSelf)
    local success, err = pcall(function()
        local name = rem.Name
        local argTable = {}
        for _, v in pairs(args) do table.insert(argTable, formatVal(v)) end
        local argStr = table.concat(argTable, ", ")
        local key = name .. "|" .. argStr
        
        -- Проверка на бан
        if blacklisted[name] and not isSelf then return end
        
        local now = tick()
        local isSpam = lastTime[name] and (now - lastTime[name] < 0.5)
        
        if not isSelf then
            callCounts[name] = (callCounts[name] or 0) + 1
            lastTime[name] = now
            if callCounts[name] > 15 then blacklisted[name] = true; return end
            if loggedKeys[key] then return end
        end
        
        loggedKeys[key] = true
        
        -- Куда пихаем кнопку
        local target = (isSpam and not isSelf) and SpamFrame or Scroll
        
        local b = Instance.new("TextButton", target)
        b.Size = UDim2.new(1, 0, 0, 25)
        b.Text = (isSelf and "[S] " or "") .. name
        b.BackgroundColor3 = isSelf and Color3.fromRGB(40, 70, 40) or (isSpam and Color3.fromRGB(80, 40, 40) or Color3.fromRGB(45, 45, 45))
        b.TextColor3 = Color3.new(1, 1, 1)
        
        b.MouseButton1Click:Connect(function()
            pcall(function()
                local path = getSafePath(rem)
                curArgs = argStr
                curScript = "game." .. path .. ":FireServer(" .. argStr .. ")"
                Details.Text = "Path: " .. path .. "\n\nArgs: " .. argStr .. "\n\nScript: " .. curScript
            end)
        end)
    end)
    if not success then warn("[QS BUG]: " .. tostring(err)) end
end

CopyArgs.MouseButton1Click:Connect(function() setclipboard(curArgs) end)
CopyScript.MouseButton1Click:Connect(function() setclipboard(curScript) end)

local mt = getrawmetatable(game)
local old = mt.__namecall
setreadonly(mt, false)

mt.__namecall = newcclosure(function(self, ...)
    local method = getnamecallmethod()
    if method == "FireServer" or method == "fireServer" then
        local args = {...}
        local isSelf = checkcaller()
        task.spawn(function() addLog(self, args, isSelf) end)
    end
    return old(self, ...)
end)
setreadonly(mt, true)
